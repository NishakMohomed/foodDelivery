// let hiddenInput = document.querySelector("#hiddenInput");
// let menu = hiddenInput ? hiddenInput.value : null;
// menu = JSON.parse(menu);

// let test = document.querySelector('.test');

// test.value = menu._id;